name: Build for Linux x64

on:
  workflow_call:
    inputs:
      tag:
        description: "tag"
        required: false
        default: ""
        type: string
  workflow_dispatch:

jobs:
  build-linux-app:
    name: Release Linux x64
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libgtk-3-dev libayatana-appindicator3-dev webkit2gtk-4.1 libasound2-dev libmpv-dev
        shell: bash

      - name: Setup flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          flutter-version-file: pubspec.yaml
          cache: true

      - name: Set and Extract version
        shell: pwsh
        run: lib/scripts/build.ps1

      - name: Apply Patch
        shell: pwsh
        run: lib/scripts/patch.ps1 Linux

      - name: Build Linux
        run: flutter build linux --release -v --pub --dart-define-from-file=pili_release.json

      - name: Package .tar.gz
        run: tar -zcvf cilicili_linux_${{ env.version }}_amd64.tar.gz -C build/linux/x64/release/bundle .

      - name: Package deb
        run: |
          printf "建立构建目录...\n"
          mkdir "cilicili_linux_${{ env.version }}_amd64"
          pushd "cilicili_linux_${{ env.version }}_amd64"
          mkdir -p opt/cilicili
          mkdir -p usr/share/applications
          mkdir -p usr/share/icons/hicolor/512x512/apps

          printf "复制文件...\n"
          cp -r ../build/linux/x64/release/bundle/* opt/cilicili
          cp -r ../assets/linux/DEBIAN .
          cp ../assets/linux/com.example.cilicili.desktop usr/share/applications
          cp ../assets/images/logo/logo.png usr/share/icons/hicolor/512x512/apps/cilicili.png

          printf "修改控制文件...\n"
          sed -i "2s/version_need_change/${{ env.version }}/g" DEBIAN/control
          SIZE_KB=$(du -s -b --apparent-size . | awk '{print int($1)}')
          SIZE_KB=$(($SIZE_KB - $(du -s -b --apparent-size DEBIAN | awk '{print int($1)}')))
          SIZE_KB=$(echo $SIZE_KB | awk '{print int($1/1024 + 0.999)}')
          printf "\t安装大小: %s KB\n" "$SIZE_KB"
          sed -i "9s/size_need_change/${SIZE_KB}/g" DEBIAN/control

          printf "生成并写入 md5sums ...\n"
          md5sum opt/cilicili/cilicili >> DEBIAN/md5sums
          md5sum opt/cilicili/lib/* >> DEBIAN/md5sums
          md5sum opt/cilicili/data/icudtl.dat >> DEBIAN/md5sums

          printf "设置权限...\n"
          chmod 0644 DEBIAN/control
          chmod 0644 DEBIAN/md5sums
          chmod 0755 DEBIAN/postinst
          chmod 0755 DEBIAN/postrm
          chmod 0755 DEBIAN/prerm

          printf "打包 deb 文件...\n"
          popd
          dpkg-deb --build --verbose --root-owner-group "cilicili_linux_${{ env.version }}_amd64"
          printf "完成: cilicili_linux_%s_amd64.deb\n" "${{ env.version }}"
        shell: bash

      - name: Package rpm
        run: |
          printf "建立 RPM 构建目录...\n"
          RPM_BUILD_ROOT="$PWD/rpm_build"
          mkdir -p "$RPM_BUILD_ROOT/BUILD" "$RPM_BUILD_ROOT/RPMS" "$RPM_BUILD_ROOT/SOURCES" "$RPM_BUILD_ROOT/SPECS" "$RPM_BUILD_ROOT/SRPMS"

          printf "准备源码归档...\n"
          DATE="$(date '+%a %b %d %Y')"
          SRC_DIR="$PWD/cilicili-${{ env.version }}"
          mkdir -p "$SRC_DIR/bundle" "$SRC_DIR/assets"
          cp -r build/linux/x64/release/bundle/* "$SRC_DIR/bundle/"
          cp assets/linux/com.example.cilicili.desktop "$SRC_DIR/assets/com.example.cilicili.desktop"
          cp assets/images/logo/logo.png "$SRC_DIR/assets/cilicili.png"
          tar -zcvf "$RPM_BUILD_ROOT/SOURCES/cilicili-${{ env.version }}.tar.gz" -C "$PWD" "cilicili-${{ env.version }}"

          printf "生成 spec 文件...\n"
          cat > "$RPM_BUILD_ROOT/SPECS/cilicili.spec" <<EOF
          Name:           cilicili
          Version:        ${{ env.version }}
          Release:        1%{?dist}
          Summary:        cilicili Linux Version
          License:        GPL-3.0
          Source0:        cilicili-${{ env.version }}.tar.gz
          Requires:       desktop-file-utils, hicolor-icon-theme

          %description
          使用 Flutter 开发的 BiliBili 第三方客户端

          %prep
          %setup -q -n cilicili-${{ env.version }}

          %build

          %install
          mkdir -p %{buildroot}/opt/cilicili
          cp -r bundle/* %{buildroot}/opt/cilicili/

          chmod 755 %{buildroot}/opt/cilicili/cilicili
          mkdir -p %{buildroot}/usr/bin
          ln -sf /opt/cilicili/cilicili %{buildroot}/usr/bin/cilicili

          mkdir -p %{buildroot}/usr/share/applications
          install -m 644 assets/com.example.cilicili.desktop %{buildroot}/usr/share/applications/com.example.cilicili.desktop

          mkdir -p %{buildroot}/usr/share/icons/hicolor/512x512/apps
          install -m 644 assets/cilicili.png %{buildroot}/usr/share/icons/hicolor/512x512/apps/cilicili.png

          %post
          update-desktop-database -q || true
          gtk-update-icon-cache -q -t -f %{_datadir}/icons/hicolor || true

          %postun
          update-desktop-database -q || true
          gtk-update-icon-cache -q -t -f %{_datadir}/icons/hicolor || true

          %files
          /opt/cilicili
          /usr/bin/cilicili
          /usr/share/applications/com.example.cilicili.desktop
          /usr/share/icons/hicolor/512x512/apps/cilicili.png

          %changelog
          * DATE - ${{ env.version }}-1
          - Initial RPM release
          EOF

          sed -i "s/DATE/${DATE}/g" "$RPM_BUILD_ROOT/SPECS/cilicili.spec"

          printf "构建 RPM 包...\n"
          rpmbuild --define "_topdir $RPM_BUILD_ROOT" -bb "$RPM_BUILD_ROOT/SPECS/cilicili.spec"

          printf "移动生成的 RPM...\n"
          find "$RPM_BUILD_ROOT/RPMS" -name "*.rpm" -exec mv {} "cilicili_linux_${{ env.version }}_amd64.rpm" \;

          printf "完成: cilicili_linux_%s_amd64.rpm\n" "${{ env.version }}"
        shell: bash

      - name: Package AppImage
        run: |
          printf "下载 appimagetool...\n"
          wget -q https://github.com/AppImage/appimagetool/releases/download/continuous/appimagetool-x86_64.AppImage
          chmod +x appimagetool-x86_64.AppImage

          printf "建立 AppDir 目录结构...\n"
          APPDIR="cilicili.AppDir"
          mkdir -p "$APPDIR/usr/bin"
          mkdir -p "$APPDIR/usr/lib"
          mkdir -p "$APPDIR/usr/share/applications"
          mkdir -p "$APPDIR/usr/share/icons/hicolor/512x512/apps"

          printf "复制应用文件...\n"
          cp -r build/linux/x64/release/bundle/* "$APPDIR/usr/bin/"

          printf "复制桌面文件和图标...\n"
          cp assets/linux/com.example.cilicili.desktop "$APPDIR/com.example.cilicili.desktop"
          cp assets/linux/com.example.cilicili.desktop "$APPDIR/usr/share/applications/com.example.cilicili.desktop"
          cp assets/images/logo/logo.png "$APPDIR/cilicili.png"
          cp assets/images/logo/logo.png "$APPDIR/usr/share/icons/hicolor/512x512/apps/cilicili.png"

          printf "创建 AppRun 启动脚本...\n"
          cat > "$APPDIR/AppRun" <<'APPRUN_EOF'
          #!/bin/bash
          SELF=$(readlink -f "$0")
          HERE=${SELF%/*}
          export PATH="${HERE}/usr/bin:${PATH}"
          export LD_LIBRARY_PATH="${HERE}/usr/lib:${LD_LIBRARY_PATH}"
          exec "${HERE}/usr/bin/cilicili" "$@"
          APPRUN_EOF
          chmod +x "$APPDIR/AppRun"

          printf "打包 AppImage...\n"
          ARCH=x86_64 ./appimagetool-x86_64.AppImage "$APPDIR" "cilicili_linux_${{ env.version }}_amd64.AppImage"

          printf "完成: cilicili_linux_%s_amd64.AppImage\n" "${{ env.version }}"
        shell: bash

      - name: Create Release
        if: ${{ github.event.inputs.tag != '' }}
        uses: softprops/action-gh-release@v3
        with:
          tag_name: ${{ github.event.inputs.tag }}
          name: ${{ github.event.inputs.tag }}
          body: |
            ## cilicili ${{ github.event.inputs.tag }} Linux x64
            
            🐧 Linux 版本
            
            ### 下载
            - **tar.gz**: cilicili_linux_*_amd64.tar.gz
            - **deb**: cilicili_linux_*_amd64.deb
            - **rpm**: cilicili_linux_*_amd64.rpm
            - **AppImage**: cilicili_linux_*_amd64.AppImage
          files: |
            cilicili_linux_*.tar.gz
            cilicili_linux_*.deb
            cilicili_linux_*.rpm
            cilicili_linux_*.AppImage

      - name: Upload linux targz package
        uses: actions/upload-artifact@v7
        with:
          archive: false
          name: Linux_targz_amd64_package
          path: cilicili_linux_*.tar.gz

      - name: Upload linux deb package
        uses: actions/upload-artifact@v7
        with:
          archive: false
          name: Linux_deb_amd64_package
          path: cilicili_linux_*.deb

      - name: Upload linux rpm package
        uses: actions/upload-artifact@v7
        with:
          archive: false
          name: Linux_rpm_amd64_package
          path: cilicili_linux_*.rpm

      - name: Upload linux AppImage package
        uses: actions/upload-artifact@v7
        with:
          archive: false
          name: Linux_AppImage_amd64_package
          path: cilicili_linux_*.AppImage
